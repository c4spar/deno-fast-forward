name: Test

on:
  push:
  pull_request:
  schedule:
    - cron: 0 0 * * *

jobs:
  test:
    name: Deno Stable
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ macOS-latest, windows-latest, ubuntu-latest ]
        deno_version: [ v1.2.0, v1.x ]
    steps:
      - name: Setup repo
        uses: actions/checkout@v2

      - name: Setup Deno
        uses: denolib/setup-deno@v2
        with:
          deno-version: ${{ matrix.deno_version }}

      - name: Setup FFmpeg
        uses: denolib/setup-ffmpeg@v1

      - name: Check FFmpeg
        run: ffmpeg -version && ffprobe -version

      - name: Run tests
        if: ${{ matrix.deno != 'v1.x' }}
        run: deno test --allow-read --allow-run --allow-write

      - name: Run tests
        if: ${{ matrix.deno == 'v1.x' }}
        run: deno test --allow-read --allow-run --allow-write --coverage=./cov

      - name: Generate lcov
        if: ${{ matrix.deno == 'v1.x' }}
        run: deno coverage --unstable --lcov ./cov > cov.lcov

      - name: Upload coverage
        if: ${{ matrix.deno == 'v1.x' }}
        uses: codecov/codecov-action@v1
        with:
          name: ${{ matrix.os }}-${{ matrix.deno }}
          files: cov.lcov
